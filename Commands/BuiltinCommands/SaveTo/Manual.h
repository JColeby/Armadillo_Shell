#pragma once
#include <string>

const std::string SaveToManual = R"(
    SaveTo Command:
        Saves a string to a file. Can pipe the output of other commands into this one.

    Syntax:
        saveto <filepath> <string>

)";
